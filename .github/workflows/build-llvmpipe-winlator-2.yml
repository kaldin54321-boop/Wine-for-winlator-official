name: Build LLVMpipe Driver (Mesa)

on:
  workflow_dispatch:
    inputs:
      mesa_version:
        description: 'Mesa version to build'
        required: true
        default: '24.1.0'

jobs:
  build-aarch64:
    # Use Ubuntu 22.04 (Jammy) for stable cross-compilation
    runs-on: ubuntu-22.04
    env:
      DEBIAN_FRONTEND: noninteractive
    steps:
      - name: Checkout Mesa
        run: |
          # Use official GitLab source to bypass GitHub mirror auth/404 issues
          git clone --depth 1 --branch mesa-${{ github.event.inputs.mesa_version }} https://gitlab.freedesktop.org/mesa/mesa.git .

      - name: Setup Multi-arch and Install Dependencies
        run: |
          sudo dpkg --add-architecture arm64
          
          # Fix: GitHub's default mirrors (azure.archive and security) DO NOT host arm64 packages.
          # We MUST restrict them to amd64 and add ports.ubuntu.com which hosts arm64 (ports).
          # This prevents the 404 errors seen in your previous logs.
          sudo sed -i 's/^deb /deb [arch=amd64] /g' /etc/apt/sources.list
          sudo find /etc/apt/sources.list.d/ -type f -name "*.list" -exec sudo sed -i 's/^deb /deb [arch=amd64] /g' {} +
          
          cat <<EOF | sudo tee /etc/apt/sources.list.d/arm64.list
          deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports jammy main restricted universe multiverse
          deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports jammy-updates main restricted universe multiverse
          deb [arch=arm64] http://ports.ubuntu.com/ubuntu-ports jammy-security main restricted universe multiverse
          EOF
          
          sudo apt-get update
          
          # Install cross-compiler and target architecture (arm64) libraries.
          # We use LLVM 15 as it's the stable default for Jammy.
          sudo apt-get install -y \
            meson ninja-build build-essential flex bison pkg-config python3-mako \
            gcc-aarch64-linux-gnu g++-aarch64-linux-gnu \
            libdrm-dev:arm64 libwayland-dev:arm64 libexpat1-dev:arm64 \
            libx11-dev:arm64 libxext-dev:arm64 libxfixes-dev:arm64 \
            libxcb1-dev:arm64 libxdamage-dev:arm64 libxshmfence-dev:arm64 \
            libxxf86vm-dev:arm64 libedit-dev:arm64 llvm-15-dev:arm64 \
            libunwind-dev:arm64 zlib1g-dev:arm64 zstd libzstd-dev:arm64

      - name: Apply Mesa Fixes for Android/Winlator
        run: |
          # 1. SHM/Memfd Fix: Android does not support POSIX SHM (/dev/shm).
          # We force Mesa to use memfd_create which is supported on Android kernels.
          # This fixes "shm_open failed" crashes in drivers.
          echo "Applying memfd/SHM patches..."
          sed -i 's/USE_POSIX_SHM/USE_MEMFD/g' src/util/os_file.c 2>/dev/null || true
          
          # 2. Add missing headers for memfd constants if needed
          sed -i '/#include <sys\/mman.h>/a #include <linux/memfd.h>' src/util/os_file.c 2>/dev/null || true

      - name: Configure Mesa (LLVMpipe)
        run: |
          # Create a cross-file pointing to the official Ubuntu arm64 sysroot
          cat <<EOF > cross-aarch64.txt
          [binaries]
          c = 'aarch64-linux-gnu-gcc'
          cpp = 'aarch64-linux-gnu-g++'
          ar = 'aarch64-linux-gnu-ar'
          strip = 'aarch64-linux-gnu-strip'
          pkgconfig = 'aarch64-linux-gnu-pkg-config'
          # Use host llvm-config but point it to target paths via env vars if needed, 
          # or let Meson handle it via the sysroot properties.
          llvm-config = '/usr/bin/llvm-config-15'

          [host_machine]
          system = 'linux'
          cpu_family = 'aarch64'
          cpu = 'armv8-a'
          endian = 'little'

          [properties]
          sys_root = '/usr/aarch64-linux-gnu'
          pkg_config_libdir = '/usr/lib/aarch64-linux-gnu/pkgconfig:/usr/share/pkgconfig'
          EOF

          meson setup build \
            --cross-file cross-aarch64.txt \
            --prefix=/usr \
            -Dplatforms=x11,surfaceless \
            -Dgallium-drivers=swrast \
            -Dvulkan-drivers=swrast \
            -Dllvm=enabled \
            -Dshared-llvm=disabled \
            -Dbuildtype=release \
            -Degl=enabled \
            -Dgbm=enabled \
            -Dopengl=true \
            -Dosmesa=true \
            -Dglx=dri

      - name: Build and Package
        run: |
          ninja -C build
          
          # --- OpenGL LLVMpipe ---
          # Structure: usr/lib/llvmpipe_dri.so (Renamed from swrast for Winlator compatibility)
          mkdir -p output/opengl/usr/lib
          if [ -f build/src/gallium/targets/dri/swrast_dri.so ]; then
            cp build/src/gallium/targets/dri/swrast_dri.so output/opengl/usr/lib/llvmpipe_dri.so
          else
            cp build/src/gallium/targets/dri/libgallium_dri.so output/opengl/usr/lib/llvmpipe_dri.so
          fi
          
          # Symlink for loader fallback
          ln -s llvmpipe_dri.so output/opengl/usr/lib/swrast_dri.so
          
          cd output/opengl && tar -cf - . | zstd -22 > ../../llvmpipe-opengl-${{ github.event.inputs.mesa_version }}.tzst && cd ..

          # --- Vulkan LLVMpipe (Lavapipe) ---
          # Structure: usr/lib/libvulkan_lvp.so + usr/share/vulkan/icd.d/lvp_icd.aarch64.json
          mkdir -p output/vulkan/usr/lib output/vulkan/usr/share/vulkan/icd.d
          cp build/src/gallium/targets/lavapipe/libvulkan_lvp.so output/vulkan/usr/lib/libvulkan_lvp.so
          
          # Generate the ICD JSON Fix (Mandatory for loading software Vulkan on Android)
          cat <<EOF > output/vulkan/usr/share/vulkan/icd.d/lvp_icd.aarch64.json
          {
              "file_format_version": "1.0.0",
              "ICD": {
                  "library_path": "../../../lib/libvulkan_lvp.so",
                  "api_version": "1.3.255"
              }
          }
          EOF
          
          cd output/vulkan && tar -cf - . | zstd -22 > ../llvmpipe-vulkan-${{ github.event.inputs.mesa_version }}.tzst && cd ..

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: llvmpipe-drivers
          path: "*.tzst"
